pipeline {
  agent any

  stages {

    stage('test') {
      echo "testing..."
        sh "ls -la"
        sh "pwd"
    }

    stage('compile') {
      echo "compiling..."
    }

    stage('build') {
      echo "building..."
    }
    
  }
}
